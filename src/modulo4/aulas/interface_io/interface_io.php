<?php
/**
 * INTERFACE I/O
 * A interface input e output se refere a como um script recebe dados (entrada) e envia dados (saída) para diferentes fontes
 * 
 * EX: requisições HTTP. $_GET (dados que vem pela URL), $_POST (dados enviados através de um form), $_FILES (arquivos enviados ao servidor). APIs...
 */